# Phase-1 reference results

Generated by `python examples/generate_phase1_results.py` using
`configs/rb87_standard_mot.yaml` and package version 0.1.0.

- Each figure is saved as a GitHub-display PNG (220 dpi) and a vector SVG from
  the same Matplotlib figure.
- `phase1_reference.npz` contains the field grid, force grid, convergence data,
  final recoil velocities, beam/magnetic sensitivity scans, and a JSON metadata
  record with units and provenance.
- The figures are internal model diagnostics, not experimental validation.
- Seed: `20260811`; Monte Carlo step: `5 ns`; maximum ensemble: `512` atoms;
  physical-coil resolution: `256` segments per loop.
